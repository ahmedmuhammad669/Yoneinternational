import { notFound } from "next/navigation";
import { PageHero } from "../../../components/page-hero";
import { PlainContent } from "../../../components/plain-content";
import { SiteShell } from "../../../components/site-shell";
import { jobBySlug } from "../../../lib/content";
export const dynamic="force-dynamic";
export default async function JobPage({params}:{params:Promise<{"job-slug":string}>}){const {"job-slug":slug}=await params;const job=await jobBySlug(slug);if(!job)notFound();const schema={"@context":"https://schema.org","@type":"JobPosting",title:job.title,description:job.description,datePosted:job.publishedAt?new Date(job.publishedAt*1000).toISOString():undefined,validThrough:job.closingDate?new Date(job.closingDate*1000).toISOString():undefined,employmentType:job.employmentType,jobLocation:job.location?{"@type":"Place","address":{"@type":"PostalAddress","addressLocality":job.location,"addressCountry":"PK"}}:undefined,hiringOrganization:{"@type":"Organization",name:"Yone International"}};return <SiteShell><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><PageHero eyebrow={job.department||"Careers"} title={job.title} description={[job.location,job.employmentType].filter(Boolean).join(" · ")||"Yone International opportunity"} breadcrumbs={[{label:"Home",href:"/"},{label:"Careers",href:"/careers"},{label:job.title}]}/><section className="section"><div className="site-container article-layout"><h2>Role</h2><PlainContent value={job.description||""}/>{job.requirements&&<><h2>Requirements</h2><PlainContent value={job.requirements}/></>}{job.applicationInstructions&&<aside className="source-note"><h2>How to apply</h2><PlainContent value={job.applicationInstructions}/></aside>}</div></section></SiteShell>;}
